[package]
name = "agentbom-engine"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Stable native engine API for AgentBOM"

[dependencies]
agentbom-core = { path = "../agentbom-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
